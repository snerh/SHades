# SHades
SH spectroscopy software

## Run

Mock full app (parameters + live plots):

```bash
julia --project=. src/examples/run_gtk_legacy_app_mock.jl
```

Real hardware full app:

```bash
julia --project=. src/examples/run_gtk_legacy_app_real.jl
```

Legacy scan console mock:

```bash
julia --project=. src/examples/run_legacy_scan_mock.jl
```
