# SHades
SH spectroscopy software

## Run

Mock full app (parameters + live plots + multithread):

```bash
julia --project=. -t 4 run_mock.jl
```

Real hardware full app:

```bash
julia --project=. -t 4 run.jl
```
